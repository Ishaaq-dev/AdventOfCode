import { expect } from 'chai';
import { getCalibrationValueSum } from './index.js';

describe('getCalibrationValueSum', () => {
    describe('should return a num of all calibration values in a txt file', () => {
        
    });
});